// src/config/api.ts
// Single source of truth for your backend URL

// When your Cloudflare worker / backend is live,
// change this to that real URL (keep the /api part if you want).
export const API_URL = "https://example.com/api";
